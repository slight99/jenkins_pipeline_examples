pipeline {
    agent any /*{ docker { image 'maven:3.3.3' } }*/
    parameters {
	string(name: 'WHAT TO SAY', defaultValue: 'Nothing', description: 'What do you want pipeline job to echo')
    }
    stages {
        stage('build') {
            steps {
                sh 'mvn --version'
            }
        }
    }
}
